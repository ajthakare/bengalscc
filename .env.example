# Bengals Cricket Club - Admin Interface Environment Variables

# Initial Admin Setup (only needed for first-time setup)
# After first admin is created, these can be removed
FIRST_ADMIN_USERNAME=admin
FIRST_ADMIN_PASSWORD=change_this_password

# Netlify Forms API Access
# Create a Personal Access Token at: https://app.netlify.com/user/applications
NETLIFY_AUTH_TOKEN=your_netlify_personal_access_token_here

# Session Secret for JWT signing
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your_random_64_character_secret_here

# Environment (set to "production" in Netlify)
NODE_ENV=development
