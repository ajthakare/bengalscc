---
import { SITE_CONFIG } from '../config';
---

<footer class="pt-8 pb-6 sm:pt-12 sm:pb-8" style="border-top: 1px solid var(--color-border);">
    <div class="text-sm text-muted space-y-3">
        <p>&copy; {new Date().getFullYear()} {SITE_CONFIG.name} &ndash; {SITE_CONFIG.location}</p>

        <div class="flex items-center gap-2 flex-wrap">
            <span class="inline-flex items-center gap-1 px-3 py-1 bg-green-50 border border-green-300 rounded-full text-xs font-semibold text-green-800">
                <span>✓</span>
                {SITE_CONFIG.nonProfit.status}
            </span>
            <span class="text-xs">EIN: {SITE_CONFIG.nonProfit.ein}</span>
        </div>

        <p>Affiliated with Northern California Cricket Association</p>
    </div>
</footer>
