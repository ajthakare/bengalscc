---
interface Props {
    name: string;
    role: string;
    image?: string;
    bio?: string;
}

const { name, role, image, bio } = Astro.props;
---

<div class="card text-center member-card-wrapper">
    <div class="mb-4 flex justify-center">
        {image ? (
            <img src={image} alt={name} class="w-24 h-24 rounded-full object-cover" loading="lazy" />
        ) : (
            <div class="w-24 h-24 rounded-full hero-bg flex items-center justify-center text-3xl font-bold text-primary">
                {name.split(' ').map(n => n[0]).join('')}
            </div>
        )}
    </div>
    <h3 class="subsection-heading heading-margin-none mb-1">
        {name}
    </h3>
    <p class="text-primary text-sm font-semibold mb-3">
        {role}
    </p>
    {bio && (
        <p class="text-muted text-sm">
            {bio}
        </p>
    )}
</div>
