---
export const prerender = false;

// Clear session cookie using Astro's cookie API
Astro.cookies.delete('bengals_admin_session', {
  path: '/'
});

// Redirect to home page
return Astro.redirect('/');
---
